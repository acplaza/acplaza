PALETTE_SIZE = 15
SIZE = (32, 32)
WIDTH, HEIGHT = SIZE
BYTES_PER_PIXEL = 4
# this isn't strictly part of the format but we put it here since it's used in multiple places by related code
MAX_DESIGN_TILES = 16
